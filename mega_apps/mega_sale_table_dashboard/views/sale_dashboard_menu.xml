<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <!-- Acciones -->
  <record id="action_sale_dashboard_facturado" model="ir.actions.client">
    <field name="name">Facturado</field>
    <field name="tag">realnet_sale_dashboard</field>
    <field name="context">{'dashboard_mode': 'facturado'}</field>
  </record>

  <!-- Menú padre (desplegable) -->
  <menuitem id="menu_sale_dashboard_root"
            name="Informes"
            parent="accountant.menu_accounting"
            sequence="999"/>

  <!-- Submenús -->
  <menuitem id="menu_sale_dashboard_facturado"
            name="Facturado"
            parent="menu_sale_dashboard_root"
            action="action_sale_dashboard_facturado"
            sequence="10"/>

</odoo>
