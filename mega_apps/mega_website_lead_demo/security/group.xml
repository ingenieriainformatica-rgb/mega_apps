<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_delete_leads_crm" model="res.groups">
        <field name="name">Allow deletion of Leads CRM</field>
        <field name="category_id" ref="base.module_category_accounting"/>
        <field name="comment">
            Permite eliminar los leads del CRM.
        </field>
    </record>

</odoo>
