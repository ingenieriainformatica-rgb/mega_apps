<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="sh_pos_all_in_one_retail.SuggestedProductList">
        <div class="product-list-container flex-grow-1 overflow-y-auto">
            <div t-if="props.products.length != 0" class="product-list d-grid gap-1 p-1">
                <!-- <t t-foreach="props.products" t-as="product" t-key="product.id"> -->
                    <ProductCard
                            t-foreach="props.products" t-as="product" t-key="product.id"
                            productId="product.id"
                            product="product"
                            class="pos.productViewMode"
                            name="getProductName(product)"
                            color="product.pos_categ_ids?.at(-1)?.color"
                            price="this.getProductPrice(product)"
                            imageUrl="pos.config.show_product_images and this.getProductImage(product)"
                            onClick.bind="() => this.addProductToOrder(product)"
                            productInfo="true"
                            onProductInfoClick.bind="() => this.onProductInfoClick(product)" />
                <!-- </t> -->
            </div>
            <div t-else="" class="product-list-empty">
                <div class="product-list-empty">
                <t t-esc="console.log('xml',props)"></t>
                    <t t-if="props.searchWord !== ''">
                        <p>
                            No results found for "
                            <b t-esc="props.searchWord"></b>
                            ".
                        </p>
                    </t>
                    <t t-else="">
                        <p>There are no products in this category.</p>
                    </t>
                </div>
            </div>
        </div>
    </t>

    <t t-extend="sh_pos_all_in_one_retail.ProductScreen" t-inherit="point_of_sale.ProductScreen" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('position-relative')]" position="inside">
            <t t-if="pos.config.enable_product_suggestion and suggestedproduct and searchWord ">
                <div class="suggestion-list" >
                    <SuggestedProductList products="suggestedproduct" searchWord="searchWord" />
                </div>
            </t>
        </xpath>
    </t>
</templates>
